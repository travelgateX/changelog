# Author node
type Author implements Node{
  # A unique identifier
  code: ID!
  # Author Data
  authorData: AuthorData!
  # Date created
  createdAt: DateTime!
  # Date updated
  updatedAt: DateTime!
}

# Author info
type AuthorData{
  # A unique identifier
  code: ID!
  # Author name
  name: String
}
